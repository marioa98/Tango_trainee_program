# Define an abstract interface (the abstract factory) to
# create shapes that we already don't know
module Shape

    # Abstract method to draw a shape.
    def draw
        "Interface not used"
    end
end